{
  "name": "my-portfolio",
  "version": "1.0.0",
  "description": "Responsive portfolio website",
  "source": "index.html",
  "scripts": {
  "dev": "parcel index.html",
  "build": "parcel build index.html"
},
  "targets": {
    "default": {
      "publicUrl": "./"
    }
  },
  "devDependencies": {
    "parcel": "^2.8.3"
  }
}
